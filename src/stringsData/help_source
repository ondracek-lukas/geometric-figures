help:source;help:so;help:open;help:o
                              so[urce] <path>
                                o[pen] <path>

                    Executes given python script file.

          In path:
            ~/... expands to the home folder
            %/... expands to the location of the application

         Application executes script %/config.py on startup
                      (after loading modules).

       For more information about scripting type :help python.
