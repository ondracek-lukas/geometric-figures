help:source;help:so
                              so[urce] <path>

                    Executes given python script file.

          In path:
            ~/... expands to the home folder
            %/... expands to the location of the application

Application executes script %/<name_of_executable>-config.py on startup.

       For more information about scripting type :help scripting.
